[tool.pytest.ini_options]
addopts = "--cov=cli --cov-report=term-missing --cov-report=html"

[tool.coverage.run]
source = ["cli"]
omit = ["cli/config.py"]

[tool.coverage.report]
fail_under = 90
show_missing = true
skip_covered = false
skip_empty = false