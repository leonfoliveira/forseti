spring:
  config:
    import: classpath:core-development.yml
server:
  cors:
    allowed-origins: ${WEBAPP_PUBLIC_URL:http://localhost:3000}
  port: ${API_PORT:8080}
  ws-port: ${WS_PORT:8081}
security:
  cookie:
    domain: ${COOKIE_DOMAIN:.forsetijudge.com}
    secure: ${COOKIE_SECURE:false}
