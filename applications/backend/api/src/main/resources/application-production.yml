spring:
  config:
    import: classpath:common-production.yml
server:
  cors:
    allowed-origins: ${WEBAPP_PUBLIC_URL:https://forseti.live}
  port: ${PORT:8080}
security:
  cookie:
    domain: ${COOKIE_DOMAIN:.forseti.live}
    secure: ${COOKIE_SECURE:true}
