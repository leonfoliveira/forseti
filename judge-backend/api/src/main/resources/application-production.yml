spring:
  config:
    import: classpath:common-production.yml
server:
  cors:
    allowed-origins: ${WEBAPP_PUBLIC_URL:https://judge.app}
  port: ${PORT:8080}
security:
  cookie:
    domain: ${COOKIE_DOMAIN:.judge.app}
    secure: ${COOKIE_SECURE:true}
