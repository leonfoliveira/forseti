spring:
  config:
    import: classpath:common-development.yml
server:
  cors:
    allowed-origins: ${WEBAPP_PUBLIC_URL:http://localhost:3000}
  port: ${PORT:8080}
security:
  cookie:
    domain: ${COOKIE_DOMAIN:.judge.app}
    secure: true
