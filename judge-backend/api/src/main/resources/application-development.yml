spring:
  config:
    import: classpath:common-development.yml
  cloud:
    aws:
      sqs:
        submission-failed-queue: judge-submission-failed-queue
server:
  cors:
    allowed-origins: http://localhost:3000
  port: 8080
