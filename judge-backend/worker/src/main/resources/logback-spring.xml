<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <timestamp key="appStartTime" datePattern="yyyy-MM-dd_HH-mm-ss"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{traceId}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <springProperty scope="context" name="lokiUrl" source="external.loki.url" />
    <springProperty scope="context" name="appName" source="spring.application.name" />
    <springProperty scope="context" name="springProfilesActive" source="spring.profiles.active" />

    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>${lokiUrl}</url>
        </http>
        <labels>
            app = ${appName}
            environment = ${springProfilesActive}
        </labels>
        <structuredMetadata>
            level=%level
            logger=%logger
            *=%%mdc
        </structuredMetadata>
        <message>
            <pattern>[%X{traceId}] %-5level %logger{36} - %msg%n</pattern>
        </message>
    </appender>

    <springProfile name="development">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="production">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="LOKI"/>
        </root>
    </springProfile>
</configuration>