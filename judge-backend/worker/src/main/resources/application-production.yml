spring:
  config:
    import: classpath:common-production.yml
server:
  port: ${PORT}
external:
  api:
    url: ${API_URL}
auto-judge:
  cpu-per-submission: ${CPU_PER_SUBMISSION}
  max-cpu: ${MAX_CPU}
  max-memory: ${MAX_MEMORY}