spring:
  config:
    import: classpath:common.yml
  application:
    name: forseti-api
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:development}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
