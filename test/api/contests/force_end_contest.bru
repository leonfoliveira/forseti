meta {
  name: force_end_contest
  type: http
  seq: 10
}

put {
  url: {{base_url}}/{{contest_id}}:force-end
  body: none
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
