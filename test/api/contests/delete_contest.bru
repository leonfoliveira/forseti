meta {
  name: delete_contest
  type: http
  seq: 11
}

delete {
  url: {{base_url}}/{{contest_id}}
  body: none
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
