meta {
  name: find_full_contest_by_id
  type: http
  seq: 8
}

get {
  url: {{base_url}}/{{contest_id}}/full
  body: none
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
