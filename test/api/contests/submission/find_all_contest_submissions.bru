meta {
  name: find_all_contest_submissions
  type: http
  seq: 2
}

get {
  url: {{base_url}}
  body: none
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
