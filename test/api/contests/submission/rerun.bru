meta {
  name: rerun
  type: http
  seq: 3
}

post {
  url: {{base_url}}/{{submission_id}}:rerun
  body: none
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
