meta {
  name: update_answer
  type: http
  seq: 2
}

put {
  url: {{base_url}}/{{submission_id}}:update-answer
  body: json
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

body:json {
  {
    "answer": "{{Submission.Answer.ACCEPTED}}"
  }
}

vars:pre-request {
  answer: {{Submission.Answer.ACCEPTED}}
}

settings {
  encodeUrl: true
  timeout: 0
}
