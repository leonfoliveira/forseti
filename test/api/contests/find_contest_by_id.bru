meta {
  name: find_contest_by_id
  type: http
  seq: 9
}

get {
  url: {{base_url}}/{{contest_id}}
  body: none
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
