meta {
  name: find_contest_metadata_by_slug
  type: http
  seq: 8
}

get {
  url: {{base_url}}/slug/{{contest_slug}}/metadata
  body: none
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
