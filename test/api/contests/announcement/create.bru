meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{base_url}}
  body: json
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

body:json {
  {
    "text": "{{$randomLoremParagraph}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
