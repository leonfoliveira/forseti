meta {
  name: delete
  type: http
  seq: 8
}

delete {
  url: {{base_url}}/{{contest_id}}
  body: none
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
