meta {
  name: find_all_contest_metadata
  type: http
  seq: 6
}

get {
  url: {{base_url}}/contests/metadata
  body: none
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
