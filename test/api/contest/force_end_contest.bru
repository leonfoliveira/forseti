meta {
  name: force_end_contest
  type: http
  seq: 12
}

put {
  url: {{base_url}}/{{contest_id}}/end
  body: none
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
