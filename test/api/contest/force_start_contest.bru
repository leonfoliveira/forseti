meta {
  name: force_start_contest
  type: http
  seq: 11
}

put {
  url: {{base_url}}/{{contest_id}}/start
  body: none
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
