meta {
  name: create_announcement
  type: http
  seq: 1
}

post {
  url: {{base_url}}
  body: json
  auth: inherit
}

body:json {
  {
    "text": "{{$randomLoremParagraph}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
