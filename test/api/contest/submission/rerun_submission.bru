meta {
  name: rerun_submission
  type: http
  seq: 7
}

post {
  url: {{base_url}}/{{submission_id}}/rerun
  body: none
  auth: inherit
}

headers {
  x-csrf-token: {{csrf_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
